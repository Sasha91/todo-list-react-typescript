import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.todolsi',
  appName: 'react-todo-list-typescript',
  webDir: 'dist'
};

export default config;
